

<div class="container">
  <div class="row">
    <a href="/post/{{id}}" class="post-list">{{title}}</a>
  </div>
    <div class="created-by">
      <span> Posted by {{user.name}} on {{format_date date_created}}</span>
    </div>
    <div class="comment-content">
      <span>{{user.comment_text}}</span>
    </div>
</div>